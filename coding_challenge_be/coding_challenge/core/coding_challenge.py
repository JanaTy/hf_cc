import logging


class CodingChallenge:

    def __init__(self, config):
        """
        Permet d'initier le contexte globale de l'application

        :param config:
        """
        self.config = config
        print("init Backend")
